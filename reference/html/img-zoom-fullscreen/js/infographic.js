$(function() {           // open in fullscreen    $('#infographic .requestfullscreen').click(function() {        var imgHeight;                if ($.fullscreen.isNativelySupported()) {           imgHeight = screen.height;        } else {           imgHeight = $(window).height();        }        $('#imgSmall').hide();        $('#imgBig').show();        $('#infoBig').smoothZoom({            width: '100%',            height: imgHeight.toString(),                    responsive: false,            responsive_maintain_ratio: false,            pan_BUTTONS_SHOW: false,            zoom_BUTTONS_SHOW: false,            pan_LIMIT_BOUNDARY : false        });        $('#infographic').fullscreen();                return false;    });    // Program zoom Buttons    $('#zoomIn').mousedown(function() {$('#infoBig').smoothZoom('zoomIn'); return false;});    $('#zoomOut').mousedown(function() {$('#infoBig').smoothZoom('zoomOut'); return false;});        // exit fullscreen zoom Button as well.    $('#zoomClose').click(function() {        $('#imgSmall').show();        $('#imgBig').hide();        $.fullscreen.exit();        return false;    });    // document's event    $(document).bind('fscreenchange', function(e, state, elem) {            // if we currently in fullscreen mode        if ($.fullscreen.isFullScreen()) {           $('#imgSmall').hide();           $('#imgBig').show();        } else {           $('#imgSmall').show();           $('#imgBig').hide();        }    });});